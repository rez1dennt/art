// Configure the receiving endpoint here. Never put SMTP passwords or secret API keys in frontend code.
export const contactConfig = Object.freeze({
  endpoint: '/api/contact',
  timeoutMs: 15000
});
